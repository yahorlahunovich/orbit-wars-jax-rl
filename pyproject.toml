[project]
name = "orbit-wars-agent"
version = "0.1.0"
description = "Cursor-ready Orbit Wars heuristic bot template"
requires-python = ">=3.11"
dependencies = [
  "kaggle-environments>=1.28.0",
  "kaggle>=1.6.17",
  "numpy>=1.24.0"
]

[tool.ruff]
line-length = 100
